# Port for the API service
API_HOST=0.0.0.0
API_PORT=8000
# Path inside the API container where model artifacts are stored
MODEL_PATH=/app/model_output

# UI settings
UI_PORT=8501
# URL of the API used by the UI
API_URL=http://api:8000

# Training hyperparameters (examples)
TRAIN_MODEL_NAME=distilbert-base-uncased
TRAIN_EPOCHS=1
TRAIN_BATCH_SIZE=16
TRAIN_LR=2e-5
